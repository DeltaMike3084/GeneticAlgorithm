package GeneticAlgorithm.Chromosome;

public class MutantChromosome extends Chromosome {
    // Chromosome built from one parent and replacement (mutation) of random genes.
    // To mutate a gene, select a gene in the chromosome at random and replace with a
    //  random gene from the genome
    // error correction of the mutated chromosome may be required
}
